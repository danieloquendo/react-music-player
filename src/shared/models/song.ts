export interface Song {
    id: number;
    name: string;

    spotifyUrl: string;
    spotify_url: string;
    
    previewUrl: string;
    preview_url: string;

    durationMs: string;
    duration_ms: string;
    explicit: string;
}